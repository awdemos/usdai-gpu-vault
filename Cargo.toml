[workspace]
members = [
    "programs/gpu_vault",
    "programs/mock_usd_ai_lend",
    "programs/mock_usd_ai_stake",
    "cli",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
